/*while (true)
 {
        cin >> input;

        if (cin.fail()) { // Tarkistaa onko syöte yhteen sopiva
            cin.clear(); // Tyhjentää virhe tilan
            cin.ignore(numeric_limits<streamsize>::max(), '\n'); // poistaa virheellisen arvon cin istä
            cout << "Invalid input. Please enter a valid integer.\n";
        }
        else
        {
            break;
        }
    }

    if(input == 1)*/